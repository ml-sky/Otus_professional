# VagrantFile разворачивает ВМ и запускает скрипт watchlog_service.sh
# Скрипт создаем сервис, который будет раз в 30 секунд мониторить лог на предмет наличия ключевого слова,
# Из epel установливается spawn-fcgi и переписывается init-скрипт на unit-файл,
# Дополняет юнит-файл apache httpd с возвожностью запуска нескольких инстантов на сервере (с разными конфигами).
# В скрипте подробные комментарии каждой из команд.