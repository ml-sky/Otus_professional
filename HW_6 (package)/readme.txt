#Vagrantfile поднимает ВМ CentOS 7 и запускает скрипт rpm_script.sh 
#Скрипт rpm_script.sh собирает свой nginx с openssl (в скрипте подробное описание всех команд)
#Также создает свой локальный репозиторий и добавляет туда собранный nginx и percona
